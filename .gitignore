# TODO(gitignore-target): target/ was committed to the repo despite this entry
# (see commit 9b5a2ca). Purge it from history with `git filter-repo --path target
# --invert-paths` and verify the entry below excludes it correctly.
# TODO(gitignore-target-tracked): also check for tracked target/ subtrees with
# `git ls-files target/` and remove any with `git rm -r --cached target/`.
/target
/fuzz/target
/fuzz/artifacts
.claude/
!.claude/agents/coursers-companion.md

# handoff-begin
.ctx/*
!.ctx/HANDOFF.*.yaml
.ctx/HANDOFF.*.state.yaml
!.ctx/handoff.*.config.toml.example
.ctx/HANDOFF.state.yaml
.ctx/.initialized
# handoff-end
.worktrees/
.DS_Store
fuzz/corpus/
